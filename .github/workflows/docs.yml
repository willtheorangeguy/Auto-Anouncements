# Documentation site for this repository.
# All build logic lives in willtheorangeguy/mkdocs.

name: Docs

on:
  push:
    branches: [main, master]
    paths:
      - 'docs/**'
      - 'overrides/**'
      - 'mkdocs.yml'
      - '.github/workflows/docs.yml'
      - 'CHANGELOG.md'
      - 'LICENSE.md'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  docs:
    uses: willtheorangeguy/mkdocs/.github/workflows/docs-build.yml@main
